<?php
/**
 * Portfolio Manager version metadata.
 *
 * Keeping the version in one file preserves the original public repo pattern
 * while also exposing constants used by the newer staged modules.
 */

declare(strict_types=1);

const PM_VERSION = '1.0.0';
const PM_VERSION_NAME = 'Portfolio Manager';

define('SITE_VERSION', PM_VERSION);
