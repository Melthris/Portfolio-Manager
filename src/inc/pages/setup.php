<?php
/**
 * Setup wizard placeholder page.
 *
 * The staged build keeps this route so the later first-run setup wizard can be
 * implemented without changing the public route map again.
 */

declare(strict_types=1);
?>
<section class="page-hero"><h1>Setup</h1><p>The setup wizard route is reserved. This staged build still seeds a temporary primary owner for review.</p></section>
